# @mediabase/bundle-ui — browser roster (mount order). Shell last.

- id: connection
  name: '@mediabase/connection'

- id: i18n
  name: '@mediabase/i18n'

- id: ui
  name: '@mediabase/ui'

# Skins: token values + the header picker. Additive — without this row the
# page renders its inline fallbacks (the classic dark shell).
- id: theme
  name: '@mediabase/theme'

- id: shell
  name: '@mediabase/ui-web'
